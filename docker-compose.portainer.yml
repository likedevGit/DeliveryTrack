# Docker Compose pour Portainer - Utilise Dockerfile.simple
services:
  deliverytrack:
    build:
      context: .
      dockerfile: Dockerfile.simple
    container_name: deliverytrack-portainer
    ports:
      - "8080:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
